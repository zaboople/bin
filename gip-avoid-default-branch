default_branch=$(git remote show origin | fgrep '  HEAD branch: ' | gawk -F 'HEAD branch:' '{print $2}'  | trim)
branch=$(gip-branch)
if [[ "$default_branch" == "$branch" && "$1" != "--force" ]]; then
  echo "NO WAY am I letting you push $branch, at least not unless you --force me to."
  exit 1
fi