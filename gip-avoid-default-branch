default_branch=$(git remote show origin | fgrep '  HEAD branch: ' | gawk -F 'HEAD branch:' '{print $2}'  | trim)
branch=$1
if [[ "$default_branch" == "$branch" && "$1" != "--master" ]]; then
  echo "NO WAY am I letting you push $branch, at least not unless you force me to via --master."
  exit 1
fi