gip-avoid-default-branch "$1" || exit 1

bra=$(gip-branch)
echo -n "Push \"$bra\"? "
read yn

yn=$(echo $yn | gawk '{print tolower($0)}')
if [[ $yn == y* ]]; then
  git push --set-upstream origin "$bra"
else
  echo 'Pfft, you weenie'
fi
